{
  "name": "@tuxounet/k2",
  "version": "1.0.0",
  "description": "K2 Build System",
  "bin": {
    "k2": "./cli.js"
  },
  "scripts": {
    "prenuke": "npm run clean",
    "nuke": "rm -rf ./node_modules ./package-lock.json",
    "clean": "rm -rf ./dist ./out ./coverage",
    "build": "tsc",
    "test": "jest --coverage",
    "samples:clean": "find ./samples/ -type f -name 'README.md' -delete ;  find ./samples/ -type f  -name 'Makefile' -delete ; find ./samples/ -type d -name 'folder' -delete",
    "samples:apply": "npm run build && npx k2 ./samples/k2.inventory.yaml apply",
    "samples:list": "npm run build && npx k2 ./samples/k2.inventory.yaml list"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.8",
    "fast-glob": "^3.2.12",
    "js-yaml": "^4.1.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.1",
    "@types/jest": "^29.2.6",
    "@types/js-yaml": "^4.0.5",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}